package com.theglendales.alarm.jjdata

object GlbVars {
    var clickedTrId: Int = -1 // 사실상 현재 재생/pause/error 중인 트랙의 ID
    var currentPlayingTrId: Int = -1
    var errorTrackId: Int = -44
    // 현재 재생 position (seekbar) ->

    // 재생 상태(StatusMP - Play/Pause/Error/Buffering 등) ->
    
    // 추후 Chip Bar 상태 ->
}