<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- C. SPLASH SCREEN (Android 12+) -->
    <style name="Theme.JJMan.Splash" parent="Theme.SplashScreen">
        <!-- Background 색 -->
        <item name="windowSplashScreenBackground">@color/icon_background_color1</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_icon_splash_test_export</item>
        <!-- 밑에 Branding Image -->
        <item name="android:windowSplashScreenBrandingImage">@drawable/avocado</item>
        <!-- Splash 보여주고 나서 적용하는 Theme-->
        <item name="postSplashScreenTheme">@style/DefaultDarkTheme</item>
    </style>
</resources>